% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/CI_PredictiveHelpers.R
\name{PredictiveScore}
\alias{PredictiveScore}
\title{Score a saved predictive model on a tfrecord}
\usage{
PredictiveScore(
  modelPath,
  file,
  imageKeysOfUnits,
  X = NULL,
  batchSize = NULL,
  conda_env = "CausalImagesEnv",
  conda_env_required = TRUE,
  Sys.setenv_text = NULL,
  atError = "stop",
  seed = NULL
)
}
\arguments{
\item{modelPath}{Path to a predictive artifact generated by \code{PredictiveRun()}.}

\item{file}{Path to a tfrecord file generated by \code{WriteTfRecord()}.}

\item{imageKeysOfUnits}{A vector of image keys to score, one per requested observation.}

\item{X}{Optional numeric matrix of transport covariates. Required when the saved
predictive model was trained with tabular covariates.}

\item{batchSize}{Optional batch size for scoring. Defaults to the value saved in the artifact.}

\item{conda_env}{A \code{conda} environment where the computational backend lives.}

\item{conda_env_required}{A Boolean stating whether use of the specified conda environment is required.}

\item{Sys.setenv_text}{Optional string for setting environment variables before Python initialization.}

\item{atError}{String specifying behavior on error. Options are \code{"stop"} (default) or \code{"debug"}.}

\item{seed}{Optional integer for reproducibility.}
}
\value{
Returns a list containing:
\itemize{
\item \code{predictedY} Predicted values for the requested observations.
\item \code{obsIndex} Observation indices corresponding to the supplied \code{imageKeysOfUnits}.
\item \code{imageKeysOfUnits} The scored image keys, returned in observation order.
}
}
\description{
Loads a predictive artifact written by \code{PredictiveRun()} and returns outcome
predictions for the requested image keys.
}
\examples{
\dontrun{
preds <- PredictiveScore(
  modelPath = "./trained_model.eqx",
  file = "./new_data.tfrecord",
  imageKeysOfUnits = new_keys
)
}

}
